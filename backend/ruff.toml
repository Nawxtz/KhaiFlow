target-version = "py312"
line-length = 100

[lint]
select = ["E", "F", "W", "I", "UP", "B", "C4"]
ignore = [
    "B008", # FastAPI Depends/Query in function definition
    "E402", # Module level import not at top of file
    "E501", # Line too long (handled by formatter)
]
